@model IEnumerable<bojan3011_ppp_projekat.Models.KategorijaVozila>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Oznaka)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Oznaka)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.KategorijaId }) |
            @Html.ActionLink("Details", "Details", new { id=item.KategorijaId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.KategorijaId })
        </td>
    </tr>
}

</table>
